# video-factory-recover：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `video-factory-recover` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 核对任务标识、最后成功阶段、远端状态、预算和授权范围。
3. 展示计划和需要的授权点。
4. 仅继续尚未完成且可证明安全的阶段。
5. 返回复用结果、新执行步骤、未恢复项和下一人工决策点。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
